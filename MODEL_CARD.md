# Spark_3D Encoder — Model Card

> Auto-generated by `log_encoder.py` on 2026-05-08 18:03

---

## Overview

| Field | Value |
|---|---|
| Model name | Spark_3D pretrained encoder |
| Architecture | 3D Dense Masked Autoencoder (SparK-style) |
| Backbone | MONAI ResNet50, 3D, dense convolutions |
| Decoder | LightDecoder3D, 4-stage hierarchical |
| Pretraining task | Patchified MSE on masked patches |
| Pretraining data | Gold_700 curated healthy T2 MRI cohort, 691 scans |
| Checkpoint file | `epoch-1480_step-256213_loss-0.00_fold-1.ckpt` |
| Best epoch | 1480 |
| Global step | 256213 |
| Generated | 2026-05-08 18:03 |

---

## Architecture Details

| Component | Specification |
|---|---|
| Backbone | MONAI 3D ResNet50 with bottleneck blocks |
| Block layers | [3, 4, 6, 3] |
| Block planes | [64, 128, 256, 512] |
| Input channels | 1 (T2-weighted MRI) |
| Spatial dims | 3D |
| Total downsample | 16× uniform across D, H, W |
| Mask ratio | 0.65 |
| Decoder | LightDecoder3D, 4 hierarchical stages |
| Decoder channels | [512 → 256 → 128 → 64 → 32] |
| Decoder fea_dim | 512 |
| Convolutions | Dense (not sparse — 3D dense MAE) |

---

## Parameter Breakdown

| Component | Parameters | % of total |
|---|---:|---:|
| 3D ResNet50 encoder | 46,211,828 | 49.6% |
| LightDecoder3D | 36,389,481 | 39.0% |
| Channel adapters (en_de_lins/norms) | 10,354,628 | 11.1% |
| Mask tokens | 3,840 | 0.0% |
| Conditioning projection (unused at pretraining) | 262,272 | 0.3% |
| **Total checkpoint** | **93,222,049** | 100% |

### Parameters by top-level component
```
{
  "model": 92959777,
  "cond_proj": 262272
}
```

---

## Training Configuration

| Setting | Value |
|---|---|
| Loss type | Patchified MSE on masked patches only |
| Per-patch normalisation | Yes (`pix_norm=1`) |
| Dense loss (all voxels) | No (`dense_loss=False`) |
| Optimiser | AdamW |
| Betas | (0.9, 0.95) |
| Weight decay | 0.05 |
| Learning rate | 1e-4 (constant — no scheduler used in this run) |
| Mask ratio | 0.65 |
| Training epochs | 1600 (best at 1480) |
| Batch size | 4 |
| Input volume | 96 × 96 × 50 (rescaleFactor=2 from 192×192×100) |
| Pad to multiple of | 16 (depth padded 50→64 internally) |

---

## Comparison to Reference (Behrendt et al. 2025 Spark_2D)

| | Behrendt's Spark_2D | This Spark_3D |
|---|---|---|
| Backbone | ResNet50 (sparse 2D) | ResNet50 (dense 3D) |
| Convolutions | SparseConv2d | Standard Conv3d |
| Loss | Patchified MSE on masked patches | Patchified MSE on masked patches |
| Per-patch normalisation | No | Yes |
| Decoder | LightDecoder, 4-level pyramid | LightDecoder3D, 4-level pyramid |
| Decoder dim | 512 | 512 |
| Pyramid levels | 4 | 4 |
| Spatial dims | 2D slices | **3D volumes** |
| Encoder parameters | ~33M (sparse ResNet50) | ~46M (dense ResNet50) |

**Key advantages of this 3D variant:**
- Captures volumetric anatomical context across slices
- Provides richer conditioning signal for the downstream 2D DDPM via FiLM
- Better suited to detecting anomalies that span multiple slices (e.g. tumours)

**Engineering simplifications vs paper:**
- Uses dense Conv3d instead of true sparse convolutions (3D sparse libraries add significant dependency cost; dense is approximate but works)
- Internal dynamic padding handles variable depth dimensions

---

## Sample Tensor Shapes

```
{
  "conv1.weight": [
    64,
    1,
    7,
    7,
    7
  ],
  "layer1.0.conv1.weight": [
    64,
    64,
    1,
    1,
    1
  ],
  "layer2.0.conv1.weight": [
    128,
    256,
    1,
    1,
    1
  ],
  "layer3.0.conv1.weight": [
    256,
    512,
    1,
    1,
    1
  ],
  "layer4.0.conv1.weight": [
    512,
    1024,
    1,
    1,
    1
  ]
}
```

---

## Loading into Stage 2 cDDPM

The checkpoint already includes a clean `encoder_state_dict` exported by the Lightning module's `on_save_checkpoint` hook. No remapping needed.

```python
import torch
from src.models.modules.spark.Spark_3D import ResNet50_3D_Backbone

ckpt = torch.load('epoch-1480_step-256213_loss-0.00_fold-1.ckpt', map_location='cpu')
encoder = ResNet50_3D_Backbone()
encoder.load_state_dict(ckpt['encoder_state_dict'], strict=True)
# Encoder ready for joint fine-tuning with DDPM_2D
```

The encoder checkpoint is loaded into the compatible `ResNet50_3D_Backbone` module before DDPM fine-tuning.

---

## Stage 2 — Joint cDDPM Training Command

```bash
python run.py model=DDPM_2D_3DEnc datamodule=Gold_700 logger=csv \
  model.cfg.encoder_path=./<path_to_logs>/logs/runs/DDPM_2D/Spark_3D_Gold_700_DDPM_2D_datamodule-Gold_700_logger-csv_model-Spark_3D_test_after_training-False_2026-05-04_16-19-52/checkpoints/epoch-1480_step-256213_loss-0.00_fold-1.ckpt \
  test_after_training=False
```

The DDPM_2D module loads `encoder_state_dict` directly — no preprocessing required.

---

## Citation

If using this encoder, please also cite the original cDDPM paper:

```bibtex
@article{BEHRENDT2025109660,
  title   = {Guided reconstruction with conditioned diffusion models for
             unsupervised anomaly detection in brain MRIs},
  author  = {Finn Behrendt et al.},
  journal = {Computers in Biology and Medicine},
  volume  = {186},
  pages   = {109660},
  year    = {2025},
  doi     = {10.1016/j.compbiomed.2025.109660}
}
```
